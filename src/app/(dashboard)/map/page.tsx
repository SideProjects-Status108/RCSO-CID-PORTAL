import { PlaceholderSection } from '@/components/ui/placeholder-section'

export default function MapPage() {
  return (
    <PlaceholderSection
      title="Field Map"
      description="A Mapbox-powered map for address lookup and call-out locations will open here. Pins stay in the browser session only and are not stored as case evidence."
    />
  )
}
