import { PlaceholderSection } from '@/components/ui/placeholder-section'

export default function TnCodePage() {
  return (
    <PlaceholderSection
      title="TN Code"
      description="Browse and search Tennessee statutes in scope for CID work, with bookmarks and recents. This module is a reference tool, not legal advice."
    />
  )
}
