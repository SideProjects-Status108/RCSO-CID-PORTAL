/**
 * Route exists for tab selection; the More menu UI is rendered by `CompanionShell`.
 */
export default function CompanionMorePage() {
  return (
    <div className="flex min-h-[40vh] flex-col items-center justify-center py-8 text-center">
      <p className="text-sm text-text-secondary">Open the menu using the tab bar below.</p>
    </div>
  )
}
